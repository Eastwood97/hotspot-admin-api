<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jsc.hotspot.db.dao.ext.HotFrontDeviceEXTMapper">
    <delete id="deleteById">
        DELETE  From hot_front_device WHERE dev_id in
        <foreach collection="devIds" item="devId" open="(" close=")" separator=",">
            #{devId}
        </foreach>
    </delete>
</mapper>